# home-manager adapter for universal.sh, mirrored 1:1.
#
# universal.sh currently defines no cross-platform preferences (its
# body is a TODO), so this module intentionally declares nothing. When
# a preference is added to the script, add its declarative mirror here.
#
# Typical homes for cross-platform user preferences:
#   programs.git.extraConfig     - Git configuration
#   programs.vscode.userSettings - VS Code settings
#   home.sessionVariables        - environment variables

{ ... }:

{
}
