id = "preferences"
trust = "first-party"
network = false
requires_sudo = false
writes = ["~/.config", "~/Library/Preferences"]
rollback = "partial"

[adapters.rcm]
path = "."

[adapters.nix-darwin]
path = "macos/nix-darwin.nix"
platforms = ["macos"]
requires_root = false
secrets = false
reboot_required = true
